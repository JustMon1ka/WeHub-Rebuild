## PART1. 项目概述

### 1. 模块概述与业务目标

* **模块名称与定位**：

  **帖子发布与展示模块**：负责用户发布多媒体内容（文字、图片、视频）及展示帖子列表，支持社交互动功能（点赞、评论、转发等）。该模块是平台内容生产的核心，直接影响用户活跃度和内容多样性。

  **帖子搜索与推荐模块**：提供内容检索和个性化推荐功能，帮助用户快速发现感兴趣的内容，提升用户留存率和平台粘性。

* **业务价值与目标**：

  **帖子发布与展示模块**：

  - 目标：为用户提供流畅的内容发布和互动体验，提升用户生成内容（UGC）的数量和质量。
  - 价值：增强社区活跃度，促进用户间互动，形成良性内容生态。
  - 成功指标：日均发帖量、互动率（点赞/评论/转发）、用户停留时长。

  **帖子搜索与推荐模块**：

  - 目标：通过精准搜索和个性化推荐，提高内容分发效率，满足用户兴趣需求。
  - 价值：提升用户满意度，增加平台使用频率。
  - 成功指标：搜索成功率、推荐内容点击率、用户留存率。

* **项目边界界定**：

  **帖子发布与展示模块**：

  - 包含：帖子发布、展示、互动（点赞/评论/转发）、标签系统、话题统计。
  - 不包含：用户账号管理（属于账号认证模块）、私信功能（属于用户社交模块）。

  **帖子搜索与推荐模块**：

  - 包含：模糊搜索、内容推荐、热门话题推荐、用户搜索。
  - 不包含：用户关注关系管理（属于用户社交模块）、圈子内容推荐（属于兴趣圈子模块）。

### 2. 页面设计与功能流程

* **页面列表及结构**：

  **帖子发布与展示模块**：

  - **帖子发布页**：
    - 功能：用户输入文字、上传图片/视频、添加标签。
    - 跳转关系：发布后跳转至帖子详情页。
  - **帖子列表页**：
    - 功能：按时间或热度展示帖子列表，支持分页加载。
    - 跳转关系：点击帖子跳转至帖子详情页。
  - **帖子详情页**：
    - 功能：展示帖子内容、评论列表、互动按钮（点赞/评论/转发）。
    - 跳转关系：点击评论跳转至评论输入框。

  **帖子搜索与推荐模块**：

  - **搜索页**：
    - 功能：提供搜索框，支持关键词搜索帖子和用户。
    - 跳转关系：点击搜索结果跳转至帖子详情页或用户主页。
  - **推荐内容页**：
    - 功能：展示个性化推荐帖子列表，支持刷新加载新内容。
    - 跳转关系：点击帖子跳转至帖子详情页。
  - **热门话题页**：
    - 功能：展示热门话题标签及关联帖子。
    - 跳转关系：点击标签跳转至话题详情页。

* **界面原型或示意图**：提供主要页面的线框图或效果图（可手绘或使用原型工具），标注关键交互元素（如按钮、输入框、导航等）和布局要点。

* **功能流程图**：

  **帖子发布操作：**

  ```mermaid
  graph TD
      A[用户点击发布按钮] --> B[进入发布页面]
      B --> C{填写内容}
      C -->|输入文字| D[支持富文本编辑]
      C -->|上传媒体| E[选择图片/视频]
      E --> F[预览媒体内容]
      F --> G{添加标签}
      G -->|手动输入| H[输入标签名称]
      G -->|选择推荐标签| I[从热门标签中选择]
      H & I --> J[点击发布按钮]
      J --> K{系统验证}
      K -->|内容合法| L[发布成功，跳转至帖子详情页]
      K -->|内容为空| M[提示“内容不能为空”]
      K -->|媒体过大| N[提示“文件大小超限”]
      M & N --> B
  ```

  **点赞/取消流程操作：**

  ```mermaid
  graph TD
      A[用户浏览帖子] --> B[点击点赞按钮]
      B --> C{当前状态}
      C -->|未点赞| D[发送点赞请求]
      D --> E[更新按钮为“已点赞”]
      C -->|已点赞| F[发送取消点赞请求]
      F --> G[更新按钮为“未点赞”]
      E & G --> H[实时更新点赞数显示]
  ```

  **评论/回复操作：**

  ```mermaid
  graph TD
      A[用户在帖子详情页] --> B{操作类型}
      B -->|点击主评论按钮| C[展开评论输入框]
      B -->|点击回复按钮| D[自动填充对目标用户]
      C --> E[输入评论内容]
      D --> E
      E --> F{内容校验}
      F -->|通过| G[提交至服务器]
      F -->|不通过| H[显示错误提示]
      H --> E
      G --> I{提交结果}
      I -->|成功| J[刷新评论列表]
      I -->|失败| K[提示 '发布失败，请重试']
      K --> E
      J --> L[显示新评论/回复]
      L --> M[高亮新增内容]
  ```

  **转发帖子：**

  ```mermaid
  graph TD
      A[用户点击转发按钮] --> B[弹窗选择转发方式]
      B -->|直接转发| C[自动生成转发内容]
      B -->|添加评论转发| D[输入附加文字]
      C & D --> E[确认转发]
      E --> F[更新原帖转发数]
      F --> G[跳转至用户个人主页]
  ```

  **收藏帖子：**

  ```mermaid
  graph TD
      A[用户点击收藏按钮] --> B{当前状态}
      B -->|未收藏| C[发送收藏请求]
      C --> D[更新按钮为“已收藏”]
      B -->|已收藏| E[发送取消收藏请求]
      E --> F[更新按钮为“未收藏”]
      D & F --> G[同步更新收藏列表]
  ```

  **模糊搜索流程：**

  ```mermaid
  graph TD
      A[用户进入搜索页] --> B[输入关键词]
      B --> C[实时显示搜索建议]
      C --> D{选择搜索类型}
      D -->|搜索帖子| E[按相关性排序结果]
      D -->|搜索用户| F[按粉丝数排序结果]
      E & F --> G[点击结果跳转至详情页]
      G -->|无结果| H[显示“暂无相关内容”]
  ```

  **内容推荐流程：**

  ```mermaid
  graph TD
      A[用户打开推荐页] --> B{推荐策略}
      B -->|基于兴趣标签| C[优先展示标签匹配内容]
      B -->|基于历史行为| D[推荐相似用户喜欢的帖子]
      C & D --> E[分页加载推荐列表]
      E --> F[用户点击帖子]
      F --> G[记录点击行为，优化下次推荐]
  ```

  **热度推荐流程：**

  ```mermaid
  graph TD
      A[系统定时统计] --> B[计算标签热度]
      B --> C[生成TOP10热门话题]
      C --> D[推送给用户]
      D --> E[用户点击话题标签]
      E --> F[展示关联帖子列表]
  ```

  ##### 关键交互说明

  1. **异常处理**：

  - 所有操作需增加网络请求失败、权限不足等异常分支（如提示“网络异常，请重试”）。

  2. **实时更新**：

  - 点赞数、评论数等需通过WebSocket或轮询实现实时更新。

  3. **数据验证**：

  - 发布/评论内容需过滤敏感词（调用风控接口），媒体文件需校验格式和大小。

## PART2. 开发中的技术细节说明

### 3. 前端组件结构与交互说明

* **组件列表与层次**：说明模块使用的 Vue 组件或页面组件，包括父子关系和职责。例如：主页面组件、表单组件、列表组件、对话框组件等。描述每个组件的作用和相互关系。
* **交互逻辑**：详述组件间的数据流和事件交互，包括 props、\$emit 事件、回调函数等。说明关键组件在用户操作时如何响应，如表单校验触发、按钮点击调用接口等。
* **样式与规范**：简述前端样式方案（如使用 CSS 预处理器、UI 框架或设计系统）和命名规范。注明是否使用 UI 组件库（Element UI、Ant Design Vue 等）并列出相关引用。

### 4. 后端接口设计
#### 4.1接口概览
* 4.1.1 模块2-帖子发布与展示模块-接口概览


  | 方法   | 接口路由             | 说明                                           |
  | ------ | -------------------- | ---------------------------------------------- |
  | POST   | /api/posts/publish   | 将写好的帖子进行上传，会在数据库中新建一行记录 |
  | POST   | /api/media/upload    | 上传帖子中将要引用的多媒体（图片，视频等）文件 |
  | GET    | /api/media/<file_id> | 从存放多媒体的服务器上获取对应的文件           |
  | POST   | /api/tags/add        | 向数据库中新添加一个tag                        |
  | GET    | /api/tags            | 根据tag_ids从数据库中批量获取对应的tag的内容   |
  | GET    | /api/tags/popular    | 获取当前的热门标签集合                         |
  | GET    | /api/posts/<post_id> | 根据post_id获取某个具体帖子的内容              |
  | GET    | /api/posts           | 根据post_ids批量获取多个帖子的内容             |
  | DELETE | /api/posts           | 根据post_id删除某个帖子                        |
  | POST   | /api/posts/like      | 点赞或者取消点赞某个帖子                       |
  | POST   | /api/posts/comment   | 发表评论或者回复                               |
  | DELETE | /api/posts/comment   | 根据comment_id删除某条评论或者回复             |
  | GET    | /api/posts/comments  | 根据post_id获取该帖子的所有评论于回复          |
  | POST   | /api/posts/forward    | 将该条转发信息存储到数据库中                   |
  | POST   | /api/posts/favorite  | 收藏某个帖子                                   |
  | GET    | /api/posts/favorites | 获取某用户的帖子收藏列表                       |
* 4.1.2 模块3-帖子搜索与推荐模块-接口概览


  | 方法 | 接口路由                   | 说明                                           |
  | ---- | -------------------------- | ---------------------------------------------- |
  | GET  | /api/posts/search          | 根据用户输入的关键词搜索出尽可能符合要求的帖子 |
  | GET  | /api/posts/search/suggests | 根据用户输入的关键词实时更新搜索推荐           |
  | GET  | /api/posts/recommends      | 根据特定的推荐算法向用户推荐帖子               |
  | GET  | /api/posts/related         | 根据某帖子的内容推荐相关的帖子                 |

#### 4.2接口详述

- 4.2.1 POST /api/posts/publish
  - 功能描述：用户在写好帖子后，通过该接口发布帖子，向数据库中新插入一行对应的内容
  - 请求头：
    - ```json
      {
        "Authorization": "Bearer <token>" 
      }
      ```
  - 请求体：
    - ```json
      {
        "content": "帖子的纯文本形式内容"
      }
      ```
  - 正常返回数据：
    - ```json
      {
        "code": 200,
        "msg": null,
        "data": {
                  "post_id": 1000001,
                  "created_at": "2025-01-01 18:00:00"
                } 
      }
      ```
  - 人为设计的请求状态码说明：
    - 200 ：OK，帖子正常发布成功
    - 400 ：Bad Request，客户端请求语法错误，可能是请求体参数不匹配
    - 401 ：Unauthorized，请求头的Authorization缺失或者token不正确
    - 405 ：Method Not Allowed，使用了不允许的请求方法（比如GET）
    - 500 ：Internal Server Error，服务端内部报错，一般用于debug

---

- 4.2.2 POST /api/media/upload

  * 功能描述：用于上传帖子中的多媒体文件，如图片、视频等，返回文件标识。
  * 请求头：

    ```json
    {
      "Authorization": "Bearer <token>",
      "Content-Type": "multipart/form-data"
    }
    ```
  * 请求体：multipart表单，包含文件字段 `file`
  * 正常返回数据：

    ```json
    {
      "code": 200,
      "msg": null,
      "data": {
        "file_id": "abc123def456"
      }
    }
    ```
  * 状态码说明：

    * 200 ：上传成功
    * 400 ：文件格式或大小不符合要求
    * 401 ：未认证用户上传
    * 500 ：服务器异常

---

- 4.2.3 GET /api/media/\<file\_id>

  * 功能描述：根据文件 ID 获取对应的媒体资源（图片/视频）。
  * 请求头：无特别要求
  * 请求体：无
  * 正常返回：媒体文件本体（Content-Type 随文件类型而定）
  * 状态码说明：

    * 200 ：成功获取文件
    * 404 ：找不到文件
    * 500 ：服务器异常

---

- 4.2.4 POST /api/tags/add

  * 功能描述：添加一个新的标签
  * 请求头：

    ```json
    {
      "Authorization": "Bearer <token>"
    }
    ```
  * 请求体：

    ```json
    {
      "tag_name": "科技"
    }
    ```
  * 正常返回：

    ```json
    {
      "code": 200,
      "msg": null,
      "data": {
        "tag_id": 123
      }
    }
    ```
  * 状态码说明：

    * 200：添加成功
    * 400：参数格式错误
    * 401：未认证
    * 500：服务器异常

---

- 4.2.5 GET /api/tags

  * 功能描述：根据 tag_ids 批量获取标签内容
  * 请求参数：URL 查询参数

    ```
    /api/tags?ids=1,2,3,......
    ```
  * 正常返回：

    ```json
    {
      "code": 200,
      "msg": null,
      "data": [
        { "tag_id": 1, "name": "科技" },
        { "tag_id": 2, "name": "生活" }
      ]
    }
    ```
  * 状态码说明：同上

---

- 4.2.6 GET /api/tags/popular

  * 功能描述：获取当前系统内使用频率较高的标签列表
  * 正常返回：

    ```json
    {
      "code": 200,
      "msg": null,
      "data": [
        { "tag_id": 5, "name": "旅游", "count": 150 },
        { "tag_id": 7, "name": "学习", "count": 120 }
      ]
    }
    ```

---

- 4.2.7 GET /api/posts/<post_id>

  * 功能描述：根据帖子ID获取帖子详情
  * 正常返回：

    ```json
    {
      "code": 200,
      "msg": null,
      "data": {
        "post_id": 100001,
        "user_id": 1111,
        "title": "帖子标题",
        "content": "这是帖子内容",
        "tags": ["tag1", "tag2", "tag3"],
        "created_at": "2025-01-01 10:00:00",
        "views": 10086,
        "likes": 10,
        "comment_count": 3
      }
    }
    ```

---

- 4.2.8 GET /api/posts

  * 功能描述：根据 post_ids 批量获取帖子内容
  * 请求参数：

    ```
    /api/posts?ids=1001,1002,1003
    ```
  * 返回：

    ```json
    {
      "code": 200,
      "msg": null,
      "data": [
        {
          "post_id": 1001,
          "user_id": 1111,
          "title": "帖子标题1",
          "content": "这是帖子内容1",
          "tags": ["tag1", "tag2", "tag3"],
          "created_at": "2025-01-01 10:00:00",
          "views": 10086,
          "likes": 10,
          "comment_count": 3
        },
        {
          "post_id": 1002,
          "user_id": 1112,
          "title": "帖子标题2",
          "content": "这是帖子内容2",
          "tags": ["tag1", "tag2", "tag3"],
          "created_at": "2025-01-01 10:00:30",
          "views": 10087,
          "likes": 100,
          "comment_count": 8
        },
        {
          "post_id": 1003,
          "user_id": 1911,
          "title": "帖子标题3",
          "content": "这是帖子内容3",
          "tags": ["tag1", "tag2", "tag3"],
          "created_at": "2025-04-01 12:00:00",
          "views": 100,
          "likes": 1,
          "comment_count": 0
        }
      ] 
    }
    ```

---

- 4.2.9 DELETE /api/posts

  * 功能描述：根据 post_id 删除指定帖子
  * 请求头：
    ```json
      {
        "Authorization": "Bearer <token>"
      }
    ```
  * 请求参数：
    ```
    /api/posts?post_id=12345
    ```
  * 正常响应：
    ```json
    {
      "code": 200,
      "msg": null,
      "data": null
    }
    ```
  * 状态码解释：
    - 200 ：删除成功
    - 400 ：参数错误
    - 401 ：身份认证未通过
    - 403 ：操作被阻止，有可能是用户尝试请求删除不属于他发布的帖子
    - 404 ：没有找到post_id对应的帖子
    - 500 ：服务器内部错误

---

- 4.2.10 POST /api/posts/like

  * 功能描述：用户对某个帖子/评论/回复 点赞或取消点赞
  * 请求头：
    ```json
      {
        "Authorization": "Bearer <token>"
      }
    ```
  * 请求体：
    ```json
    {
      "type": "post/comment/reply",
      "target_id": 1001,
      "like": true
    }
    ```
  * 正常响应：
    ```json
    {
      "code": 200,
      "msg": null,
      "data": null
    }
    ```
  * 状态码解释：
    - 200 ：成功
    - 400 ：参数错误
    - 401 ：身份认证未通过
    - 404 ：没有找到target_id对应的帖子
    - 500 ：服务器内部错误

---

- 4.2.11 POST /api/posts/comment

  * 功能描述：发表评论或对评论进行回复
  * 请求头：
    ```json
        {
          "Authorization": "Bearer <token>"
        }
    ```
  * 请求体：
    ```json
    {
      "type": "comment/reply",
      "target_id": 1001,
      "content": "写得真好！"
    }
    ```
  * 正常响应：
    ```json
    {
      "code": 200,
      "msg": null,
      "data": {
              "comment_id/reply_id": 1192,
              "created_at": "2025-04-01 12:00:00"
            } 
    }
    ```

---

- 4.2.12 DELETE /api/posts/comment

  * 功能描述：删除评论或回复
  * 请求头：
    ```json
        {
          "Authorization": "Bearer <token>"
        }
    ```
  * 请求参数：

    ```
    /api/posts/comment?type=comment/reply&target_id=1234
    ```

---

- 4.2.13 GET /api/posts/comments

  * 功能描述：获取指定帖子的评论与回复
  * 请求头：
    ```json
        {
          "Authorization": "Bearer <token>"
        }
    ```
  * 请求参数：

    ```
    /api/posts/comments?post_id=1001
    ```
  * 正常响应：
    ```json
    {
      "code": 200,
      "msg": null,
      "data": [
        {
          "type": "comment",
          "comment_id": 1999,
          "user_id": 4444,
          "created_at": "2025-04-01 12:00:00",
          "content": "评论内容1",
          "likes": 108
        }, 
        {
          "type": "reply",
          "reply_id": 1302,
          "user_id": 1234,
          "created_at": "2025-04-01 12:30:00",
          "content": "[reply_type: comment, id=1999] 回复内容1",
          "likes": 10
        }, 
        {
          "type": "reply",
          "reply_id": 1303,
          "user_id": 4444,
          "created_at": "2025-04-01 12:40:00",
          "content": "[reply_type: reply, id=1302][reply_type: comment, id=1999] 回复内容2",
          "likes": 2
        } 
      ] 
    }
    ```

---

- 4.2.14 POST /api/posts/forward

  * 功能描述：转发帖子
  * 请求头：
    ```json
        {
          "Authorization": "Bearer <token>"
        }
    ```
  * 请求体：

    ```json
    {
      "post_id": 1001,
      "comment": "转发一下~"
    }
    ```
  * 正常响应：
    ```json
    {
      "code": 200,
      "msg": null,
      "data": {
                "repost_id": 1000,
                "created_at": "2025-04-01 12:00:00"
              }
    }
    ```

---

- 4.2.15 POST /api/posts/favorite

  * 功能描述：收藏帖子
  * 请求头：
    ```json
    {
      "Authorization": "Bearer <token>"
    }
    ```
  * 请求体：

    ```json
    {
      "post_id": 1001
    }
    ```

---

- 4.2.16 GET /api/posts/favorites
  * 功能描述：获取用户收藏的帖子列表
  * 请求头：
    ```json
    {
      "Authorization": "Bearer <token>"
    }
    ```
  * 返回：
    ```json
    {
      "code": 200,
      "msg": null,
      "data": [
        { "post_id": 1001, "title": "好帖子", "tags": ["tag1", "tag2", "tag3"]},
        { "post_id": 1002, "title": "推荐阅读", "tags": ["tag1", "tag2", "tag3"]}
      ]
    }
    ```

---

- 4.2.17 GET /api/posts/search

  * 功能描述：根据关键词搜索帖子
  * 请求参数：

    ```
    /api/posts/search?keyword=AI&limit=10
    ```
  * 返回：
    ```json
    {
      "code": 200,
      "msg": null,
      "data": [
        { "post_id": 1001, "title": "好帖子", "tags": ["tag1", "tag2", "tag3"]},
        { "post_id": 1002, "title": "推荐阅读", "tags": ["tag1", "tag2", "tag3"]},
        ...(共十条数据)
      ]
    }
    ```
  

---

- 4.2.18 GET /api/posts/search/suggests

  * 功能描述：根据用户输入实时返回关键词建议
  * 请求参数：

    ```
    /api/posts/search/suggests?query=机器&limit=10
    ```
  * 返回：
    ```json
      {
        "code": 200,
        "msg": null,
        "data": [
          "机器人大战人工队",
          "机器人会取代哪些职业",
          ...(共十条数据)
        ]
      }
    ```

---

- 4.2.19 GET /api/posts/recommends

  * 功能描述：为用户推荐帖子
  * 请求参数：
    ```
    /api/posts/recommends?user_id=123&limits=10
    ```
  * 正常返回：
    （与/api/posts/search返回数据格式一致）

---

#### 4.2.20 GET /api/posts/related

  * 功能描述：获取与某帖子内容相关的其他帖子推荐
  * 请求参数：

    ```
    /api/posts/related?post_id=1001
    ```
    * 正常返回：
      （与/api/posts/search返回数据格式一致）

### 5. 数据库设计

* **数据表概览**：列出与模块相关的数据库表（含表名及简要用途）。如果是新表或改动表，应提供表结构设计。
* **字段定义**：使用表格列明每个表的字段信息，包括字段名称、数据类型、长度、字段含义、约束（主键、自增、外键、唯一等）、是否允许空、默认值、备注等。例如：


| 字段名       | 类型        | 说明     | 约束         | 备注 |
| ------------ | ----------- | -------- | ------------ | ---- |
| `Id`         | int(11)     | 主键     | 主键、自增   |      |
| `UserName`   | varchar(50) | 用户名   | 唯一、非空   |      |
| `CreateTime` | datetime    | 创建时间 | 默认当前时间 |      |

* **数据关系**：绘制 ER 图或说明表之间的关联关系（外键关联）。指出与其他模块共享的表或字段，并注明数据约束和索引策略。

#### 5.1 数据表概览
- 5.1.1 Post表
  |字段名	|数据类型	|长度（B）	|说明	|备注|
  | ---------- | ---------- | ------- | ----- | ----- |
  |POST_ID	|NUMBER(*,0)	|1-22	|帖子唯一标识	|PK|
  |TITLE	|VARCHAR2	|200	|帖子标题|
  |CONTENT	|CLOB	|<=128M	|帖子内容|
  |CREATED_AT	|DATE	|7	|创建时间|
  |IS_HIDDEN	|NUMBER(1,0)|	1|	是否隐藏|
  |IS_DELETED	|NUMBER(1,0)	|1	|是否被删除|
  |VIEW_COUNT	|NUMBER(*,0)	|1-22	|查看数|
  |LIKE_COUNT	|NUMBER(*,0)	|1-22	|点赞数|
  |DISLIKE_COUNT	|NUMBER(*,0)	|1-22	|点踩数|
  |POSTER_ID	|NUMBER(*,0)	|1-22	|发布者ID	|FK|
- 5.1.2 Tag表
  | 字段名  | 数据类型       | 长度（B） | 说明         | 备注 |
  |---------|----------------|-----------|--------------|------|
  | TAG_ID  | NUMBER(*,0)   | 1-22      | 标签唯一标识 | PK   |
  | NAME    | VARCHAR2      | 200       | 标签名       |      |
- 5.1.3 PostTag表
  | 字段名   | 数据类型     | 长度（B） | 说明     | 备注                |
  |----------|--------------|-----------|----------|---------------------|
  | post_id  | NUMBER(*,0) | 1-22      | 帖子ID   | FK                  |
  | tag_id   | NUMBER(*,0) | 1-22      | 标签ID   | FK                  |
  |          |              |           |          | PRIMARY KEY(post_id, tag_id) |
- 5.1.4 Forward表
  | 字段名       | 数据类型       | 长度（B） | 说明         | 备注 |
  |--------------|----------------|-----------|--------------|------|
  | FORWARD_ID   | NUMBER(*,0)   | 1-22      | 活动唯一标识 | PK   |
  | COMMENT      | CLOB          | <=128T    | 评论         |      |
  | CREATE_AT    | DATE          | 7         | 创建时间     |      |
  | FORWARDER_ID | NUMBER(*,0)   | 1-22      | 转发者ID     | FK   |
- 5.1.5 Comment表
  | 字段名         | 数据类型       | 长度（B） | 说明           | 备注 |
  |----------------|----------------|-----------|----------------|------|
  | COMMENT_ID     | NUMBER(*,0)   | 1-22      | 评论唯一标识   | PK   |
  | POST_ID        | NUMBER(*,0)   | 1-22      | 评论所属的帖子的id | FK |
  | CONTENT        | CLOB          | <=128T    | 内容           |      |
  | LIKE_COUNT     | NUMBER(*,0)   | 1-22      | 点赞数         |      |
  | DISLIKE_COUNT  | NUMBER(*,0)   | 1-22      | 点踩数         |      |
  | IS_DELETED     | NUMBER(1,0)   | 1         | 是否被删除     |      |
  | COMMENTER_ID   | NUMBER(*,0)   | 1-22      | 评论发表者ID   | FK   |
- 5.1.6 Reply表
  | 字段名       | 数据类型       | 长度（B） | 说明           | 备注 |
  |--------------|----------------|-----------|----------------|------|
  | REPLY_ID     | NUMBER(*,0)   | 1-22      | 回复唯一标识   | PK   |
  | COMMENT_ID   | NUMBER(*,0)   | 1-22      | 回复的评论的id | FK   |
  | CONTENT      | CLOB          | <=128T    | 内容           |      |
  | IS_DELETED   | NUMBER(1,0)   | 1         | 是否被删除     |      |
  | REPLIER_ID   | NUMBER(*,0)   | 1-22      | 回复发布者ID   | FK   |
- 5.1.7 Media表
  | 字段名   | 数据类型     | 长度（B） | 说明         | 备注 |
  |----------|--------------|-----------|--------------|------|
  | MEDIA_ID | NUMBER(*,0) | 1-22      | 媒体唯一标识 | PK   |
  | TYPE     | VARCHAR2    | 10        | 媒体类型     |      |
  | URL      | VARCHAR2    | 255       | 媒体URL      |      |
- 5.1.8 Like表
  | 字段名       | 数据类型     | 长度（B） | 说明     | 备注                      |
  |--------------|--------------|-----------|----------|---------------------------|
  | user_id      | NUMBER(*,0) | 1-22      | 用户ID   | FK                        |
  | target_type  | VARCHAR2    | 10        | 类型（帖子/评论） |                           |
  | type         | NUMBER(1,0) | 1         | 点赞还是点踩                           |  |
  | target_id    | NUMBER(*,0) | 1-22      | 目标ID   | FK                        |
  |              |             |           |          | PRIMARY KEY(user_id, target_id) |

### 6. 权限与用户角色控制

* **角色定义**：列出涉及该模块的用户角色（如管理员、普通用户、财务员等）以及各角色的权限范围。说明哪些角色可以访问此模块、执行哪些操作。
* **权限策略**：采用基于角色的访问控制（RBAC）模型，将权限授权与角色关联。例如，可用表格说明角色-权限映射：


| 角色     | 权限操作                    |
| -------- | --------------------------- |
| 管理员   | 查看/新增/修改/删除全量数据 |
| 财务员   | 查看/导出报表               |
| 普通用户 | 查看个人信息                |

* **前后端控制**：说明前端页面的菜单/按钮显示权限（如路由权限），以及后端接口的权限校验方式（如基于 JWT 的身份验证或权限注解）。

### 7. 第三方依赖与集成点说明

* **依赖服务清单**：列出该模块依赖的所有外部服务或组件，例如短信服务、支付网关、文件存储、消息队列、日志系统等。并说明所使用的第三方平台或库（如阿里云短信、微信支付、AWS S3、RabbitMQ 等）。
* **集成方式**：针对每个第三方服务，说明接入方式和关键点，例如调用的 API 接口、使用的 SDK、认证方式、回调机制、许可证需求等。提供文档链接或参考。
* **关键配置**：列出所需的系统配置项，如 API 密钥、秘钥存储位置、WebHook 回调地址、超时时间和重试策略等。

## PART3. 开发策略与约定

### 8. 状态管理方案

* **全局状态管理**：说明前端状态管理方案，如使用 Vuex（或 Pinia）进行集中式状态管理。Vuex 是 Vue.js 的官方状态管理库，采用集中式存储管理组件状态，确保状态变化可预测。
* **模块状态划分**：描述各状态仓库模块（store module）对应的功能域，例如用户信息、订单数据、全局配置等。列出每个状态模块包含的 state、getter、mutation、action 的主要字段。
* **数据流动示意**：说明前端组件如何通过 dispatch/commit 操作全局状态，状态更新流程，及与后端接口的数据交互。例如：登陆成功后 `commit('setUser', userInfo)` 更新用户状态。

### 9. 输入校验、异常处理与边界场景考虑

* **输入验证**：前后端需共同验证输入数据的合法性，包括必填字段、数据格式、长度范围、值域限制等。说明采用的验证规则（正则表达式、枚举、范围校验等），例如邮箱格式、密码长度等。针对表单和 API 接口均需描述校验逻辑。
* **异常处理**：定义错误处理策略：接口返回的状态码规则（如 4xx 表示客户端错误，5xx 表示服务器错误等）；应用层抛出的错误信息规范；前端友好提示方案。列举可能的异常场景，如数据库约束冲突、缓存失效、调用第三方失败等，并说明处理方式（重试、降级或返回特定错误码）。
* **边界与特殊情况**：考虑边界条件和异常输入。例如，输入超出允许范围、缺失必填字段、数据类型不匹配、特殊字符/注入攻击等。对这些情况应返回明确的错误信息（通常为 4xx 错误码）并记录日志。还应考虑并发冲突（如重复提交、乐观锁冲突返回 409）和系统负载高时的退避处理（如 429 限流）。

### 10. 开发约定与注意事项

* **编码规范**：[Typescript](https://google.github.io/styleguide/tsguide.html) | [C#](https://google.github.io/styleguide/csharp-style.html) | [Others](https://google.github.io/styleguide/)
* **开发流程**：说明代码仓库主分支（master）为当前最新全功能分支（需保证任何时候都可用），余下为各开发小组的开发分支（不保证任何时候都可用，由开发小组内部规定），提交信息规范（[多人提交冲突参考](https://zhuanlan.zhihu.com/p/584834288)）：
```txt
新增：
1、<文件路径>：<做了什么>
...
修改：
1、<文件路径>：<做了什么>
...
删除：
1、<文件路径>：<做了什么>
...
```
* **环境配置**：.NET 8.0.412版本、pnpm 10.10.0、Node 22.15.0。
* **前端兼容**：Chrome/Edge、Firefox、Safari。
* **日志与监控**：约定日志格式、日志级别使用（Info/Warning/Error），及错误监控告警方式。

### 11. 接口联调与 Mock 策略

* **联调计划**：说明后端接口开发完成后的联调流程，如接口测试完成标准、前后端联调会议、接口稳定性确认等。列出用于联调的测试账号和接口文档链接。
* **Mock 数据方案**：在后端接口尚未就绪时，可使用 Mock 技术模拟接口返回数据，以提升开发效率。例如，可在 Vue 项目中使用 Mock.js 或 JSON 文件提前构造测试数据，使前端开发与接口定义并行进行。文档中可说明 Mock 服务的搭建方式和数据格式规范。
* **测试**：在`/Tests/`下建立对应的测试，写好对应的测试流程。